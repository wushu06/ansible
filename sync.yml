- hosts: web
  tasks:
  - name: "sync website"
    synchronize:
      src: site/
      dest: /var/www/html/current
      archive: no
      checksum: yes
      recursive: yes
      delete: yes
    become: no
